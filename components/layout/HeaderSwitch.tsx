"use client";

import { Header } from "@/components/layout/Header";

/**
 * v2: Der Header liegt auf keiner Seite mehr ueber einem Bild,
 * sondern ist ueberall eine feste Elfenbein-Leiste mit Hairline.
 */
export function HeaderSwitch() {
  return <Header />;
}
